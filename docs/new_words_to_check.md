# New Words to Check

Output of the adding_new_word skill. When a new word fails LLM-vs-engine comparison (mismatched or missing forms), it is logged here for human review instead of being added to the seed.
